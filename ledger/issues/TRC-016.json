{
  "id": "TRC-016",
  "title": "A Claude Code local command ends the idle interval, though the agent never resumed",
  "type": "defect",
  "severity": "med",
  "status": "closed",
  "foundBy": "session agent, looking for the elapsed time that R70 still left on no event in an attended Claude Code session",
  "opened": "2026-09-21",
  "resolved": "2026-09-21",
  "stint": "2026-09-21 time accounting",
  "evidence": {
    "commits": [
      "23b6196",
      "092d355"
    ],
    "integrated": [],
    "verified": "`make check` passes with parity enabled. A new test on raw records asserts that an echo leaves a two hour wait whole, and that a hand-back still ends one; it fails without the fix.\n\nThe goldens moved in twelve files, checked structurally. Eleven echoes changed kind, the idle and work intervals around them moved, and two derived titles changed, with nothing else.\n\nOn the attended session that exposed it, elapsed time on no event fell from 6.5% of 332 hours to 1.7%."
  },
  "resolution": "The Claude Code adapter now reports the echo of a local command as a system event, beside the command that produced it. It recognizes the echo by `isMeta` together with a `local_command` parent, so a hand-back or a task notice still reads as an instruction. The shared idle pass is unchanged. R68 gained a sentence saying that an adapter reports a user instruction only for a message the agent is meant to act on.\n\nTwo fixture sessions opened with an echo, so their derived title now comes from the first message the user sent.",
  "details": "R68 ends the idle interval at a user instruction. The Claude Code adapter reads every `user` record as one, including the ones Claude Code writes itself to echo a local command. Running `/context` or `/cd` while the agent waits writes such a record, and the agent does not reply to it.\n\nSo the wait stops at the command, and the rest of it, up to the next real instruction, is neither idle nor work. In one attended session two such commands left 20.5 hours of 332 on no event. A reader sees a trajectory that was busy while nobody was there.\n\nThe echo carries `isMeta` and names the `local_command` record as its parent. Other records carrying `isMeta` do start work, such as a sub-agent's hand-back or a background-task notice, so the flag alone does not separate them.\n\nTo reproduce, normalize a Claude Code session in which `/context` ran between turns, and compare the turn end's `idleMs` with the gap to the next real instruction.",
  "notes": [],
  "links": [
    "TRC-014",
    "TRC-015"
  ]
}
