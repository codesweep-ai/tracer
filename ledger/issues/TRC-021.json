{
  "id": "TRC-021",
  "title": "The fixture suite runs in no gate, so five of its expectations fell behind the changes they measure",
  "type": "defect",
  "severity": "med",
  "status": "closed",
  "foundBy": "session agent, running the whole fixture suite after adding a check for card marks",
  "opened": "2026-09-21",
  "resolved": "2026-09-21",
  "stint": "2026-09-21 time accounting",
  "evidence": {
    "commits": [
      "f7c33bf",
      "58bc0e8"
    ],
    "integrated": [],
    "verified": "`make ci` passes, and its fixture-suite step reports 35 checks with 26 matching and 9 targets met. With no browser variable set and no `/usr/bin/chromium-browser`, `make fixtures` prints its SKIP line and exits 0. A timed run took 54 seconds against 20 for a warm `make check`."
  },
  "resolution": "`make ci` now runs the fixture suite after the ledger check, and `make check` still does not, because the suite takes about a minute. Like parity, it skips with a line saying why where there is no npm or no browser. SPEC lists it among the gates, and CONTRIBUTING counts it among those that can skip.",
  "details": "The fixture suite in `apps/viewer/fixtures` drives the exported pages in a browser and compares 35 measurements against `expectations.json`. Neither `make check` nor `make ci` runs it, so a change can pass every gate while the suite fails.\n\nFive checks fail on the current tree, and each failure is an intended change the expectations never recorded. The index rollup still expects the token and cost totals from before the cost fixes. The kind chips predate a local command's echo becoming a system event. Two keyboard checks predate the raw JSON toggle on tool inputs, and the data size predates the time and cost fields.\n\nA suite nobody runs cannot catch anything. The first real defect it could have caught was a card mark four pixels out of line, and it only caught that once somebody ran it by hand.\n\nTo reproduce, run `make fixtures` with a browser configured.",
  "notes": [
    {
      "date": "2026-09-21",
      "text": "The five expectations were recorded in f7c33bf, each row carrying its approval, and the whole suite passes. The Makefile keeps the suite out of `make check` on purpose, as the oracle for a component migration. Whether it becomes a gate is the maintainer's decision, so the record stays open."
    },
    {
      "date": "2026-09-21",
      "text": "The maintainer chose `make ci` over `make check`. The GitHub workflow's build job installs neither Node nor a browser, so it skips both parity and the fixture suite. They run in `make ci` on a machine with a browser."
    }
  ],
  "links": [
    "TRC-016",
    "TRC-011",
    "TRC-017",
    "TRC-018",
    "TRC-020"
  ]
}
